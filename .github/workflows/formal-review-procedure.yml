name: Formal review procedure

on:
  pull_request:
    paths:
      - 'src/**'
      - 'tests/**'
      - '.cli-flags.toml'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'docs/FORMAL_REVIEW_PROCEDURE.md'
      - 'formal/PROCEDURE.md'
      - 'formal/procedure.toml'
      - 'formal/review-procedure/**'
      - '.github/workflows/formal-review-procedure.yml'
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'tests/**'
      - '.cli-flags.toml'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'docs/FORMAL_REVIEW_PROCEDURE.md'
      - 'formal/PROCEDURE.md'
      - 'formal/procedure.toml'
      - 'formal/review-procedure/**'
      - '.github/workflows/formal-review-procedure.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  obligations:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Validate formal obligation register
        run: python3 formal/review-procedure/check.py

      - name: Exhaust bounded domain model
        run: python3 -O formal/review-procedure/model.py
