name: Format global package branch once

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write

concurrency:
  group: format-global-package-branch
  cancel-in-progress: false

jobs:
  format:
    if: >-
      github.event.pull_request.head.ref == 'agent/global-zed-packages' &&
      github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out the PR branch rather than the merge ref
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          persist-credentials: true
          fetch-depth: 0

      - name: Install pinned Rust formatter
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
        with:
          toolchain: stable
          components: rustfmt

      - name: Apply formatter and Clippy lock annotation
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('src/global.rs')
          text = path.read_text()
          old = '''        .create(true)
                  .read(true)
                  .write(true)
                  .open(root.join(LOCK_FILE))?;'''
          new = '''        .create(true)
                  .truncate(false)
                  .read(true)
                  .write(true)
                  .open(root.join(LOCK_FILE))?;'''
          if old not in text:
              raise SystemExit('expected lock-file OpenOptions chain not found')
          path.write_text(text.replace(old, new, 1))
          PY
          cargo fmt --all

      - name: Commit formatter output
        shell: bash
        run: |
          if git diff --quiet -- src/global.rs src/completion.rs; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/global.rs src/completion.rs
          git commit -m 'style: apply Rust formatting'
          git push origin HEAD:agent/global-zed-packages
