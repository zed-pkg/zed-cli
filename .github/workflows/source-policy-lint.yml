# Managed by ores-otel source-policy fleet rollout.
name: Pre-build source policy lint

on:
  pull_request:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: source-policy-lint-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-policy:
    uses: ores-otel/.github/.github/workflows/source-policy-lint.yml@a9f82646e72c698ca7f99773c6b4d96395bb9020
    permissions:
      contents: read
