# DEN-3488 v0.3.0 integration receipt

## Immutable composition

The finite exact-version circular-dependency implementation has been reconciled with released `zed-cli` v0.3.0 through a true two-parent merge.

- v0.3.0 main: `b38a1edd2ca7852bc81ad2e01e2989f44009e93e`
- pre-integration DEN-3488 head: `1aa85862e897a79b084b2a1be908e6b7704f7664`
- semantic merge: `44b926349cb8a45d8a8867af69dd601c50f3ce54`
- regenerated-lock head: `c218781a5fd3d92c75d6bdad6773e8e13bb4f929`

GitHub compare reports the regenerated-lock head ahead-only on v0.3.0 main. The effective product delta is limited to the exact graph implementation, public graph command, focused workflow/tests, documentation, the immutable ORES logger dependency, and its regenerated lock graph.

## Semantic conflict decisions

- Preserve v0.3.0 package version, current `zed-interfaces`, `zed-lock`, `flags-2-env`, CLI, mirror, publication, and release behavior.
- Add `next_loggers` only for structured, terminal-visible circular-dependency diagnostics.
- Keep the current install graph's registry ownership; an obsolete test-only import exposed by all-target compilation was removed rather than suppressing the warning.
- Keep ordinary `.zpkg.lock` v1 one-version-per-coordinate semantics. The new materializer consumes an explicit exact resolved graph; automatic solver fallback remains a separate contract migration.
- Preserve the already-merged, self-hosted Claritas graph visualization in `zed-web-server.rs`; this CLI change does not add a duplicate browser runtime.

## Exact validation

Bounded workflow run `33909111236` completed successfully before publishing the regenerated lockfile and deleting itself. It proved:

- `cargo generate-lockfile` resolved `zed-cli` 0.3.0;
- the lock includes `oresoftware-next-loggers` at exact revision `9e28eca35b4c3f283cde933ae645216972d2f3e3`;
- `cargo fmt --all -- --check` passed;
- `cargo check --locked --all-targets` passed;
- the complete `versioned_graph` unit suite passed;
- the public graph-materialization parser regression passed;
- the executable finite-cycle integration suite passed;
- strict all-target Clippy passed with only the three narrowly documented current-main lint exceptions used by the original certified DEN-3488 workflow.

The one-shot workflow is absent from the review tree. This receipt commit intentionally triggers the permanent repository pull-request workflows on the exact current product head.

## Promotion boundary

Do not merge based solely on this receipt. Every permanent workflow on the exact current PR head must be terminal and green, independent review must be complete, and companion `zed-pkg-test` acceptance must pin the resulting immutable product revision. Pending, skipped, canceled, older-head, or zero-job runs are not passing evidence.
