// Generated by flags-2-env from .cli-flags.toml. Do not edit.
#![allow(dead_code)]

/// Runtime values read from a lookup of env keys.
#[derive(Clone, Debug, PartialEq)]
pub struct CliEnvValues {
    pub adapter: String,
    pub allow_build: bool,
    pub allow_dirty: bool,
    pub allow_ecosystem_mismatch: bool,
    pub allow_install_hooks: bool,
    pub allow_native_deps: bool,
    pub auth_url: Option<String>,
    pub check: bool,
    pub classpath: Option<String>,
    pub clean: bool,
    pub cli: Option<String>,
    pub cli_install_mode: String,
    pub cli_target: Option<String>,
    pub comspec: Option<String>,
    pub display_name: Option<String>,
    pub do_not_write_new_manifest: bool,
    pub docker: bool,
    pub dry_run: bool,
    pub email: Option<String>,
    pub env_config: Option<String>,
    pub env_json: bool,
    pub env_lock: Option<String>,
    pub force: bool,
    pub frozen: bool,
    pub git_submodules: bool,
    pub home: Option<String>,
    pub image: String,
    pub in_nix_shell: Option<String>,
    pub install_mode: String,
    pub interactive: bool,
    pub limit: Option<i64>,
    pub name: Option<String>,
    pub native_manager: Option<String>,
    pub nix_build_top: Option<String>,
    pub older_than: String,
    pub org: Option<String>,
    pub out: Option<String>,
    pub password_stdin: bool,
    pub provider: String,
    pub pythonpath: Option<String>,
    pub r2g_registry_mode: String,
    pub r2g_root: Option<String>,
    pub registry: String,
    pub runtime: Option<String>,
    pub skip_checksum: bool,
    pub skip_vcs_checks: bool,
    pub supabase_key: Option<String>,
    pub supabase_url: Option<String>,
    pub target: Option<String>,
    pub token: Option<String>,
    pub undo: bool,
    pub xdg_config_home: Option<String>,
    pub zed_pkg_auth_password: Option<String>,
    pub zed_pkg_install_concurrency: Option<String>,
    pub zed_pkg_layout_config: Option<String>,
    pub zed_pkg_max_artifact_bytes: Option<String>,
    pub zed_pkg_max_binary_entries: Option<String>,
    pub zed_pkg_max_registry_bytes: Option<String>,
    pub zed_pkg_max_unpacked_bytes: Option<String>,
    pub env_check: bool,
    pub env_output: Option<String>,
    pub env_plan: Option<String>,
    pub env_receipt: Option<String>,
    pub env_write: bool,
    pub zed_pkg_gc_dry_run: bool,
    pub release_channel: String,
    pub release_iteration: i64,
    pub release_json: bool,
    pub zed_pkg_update_force: bool,
    pub json: bool,
    pub plan: Option<String>,
    pub all: bool,
    pub zed_task_dry_run: bool,
    pub jobs: i64,
    pub no_cache: bool,
    pub yes: bool,
    pub zed_pkg_validate_json: bool,
    pub lock: String,
    pub manifest: String,
    pub require_lock: bool,
}

/// Pure: resolve values from an explicit lookup.
pub fn load_from(lookup: impl Fn(&str) -> Option<String>) -> CliEnvValues {
    CliEnvValues {
        adapter: lookup("ZED_PKG_ADAPTER").filter(|value| !value.is_empty()).unwrap_or_else(|| "auto".to_string()),
        allow_build: parse_bool(lookup("ZED_PKG_ALLOW_BUILD"), false),
        allow_dirty: parse_bool(lookup("ZED_PKG_ALLOW_DIRTY"), false),
        allow_ecosystem_mismatch: parse_bool(lookup("ZED_PKG_ALLOW_ECOSYSTEM_MISMATCH"), false),
        allow_install_hooks: parse_bool(lookup("ZED_PKG_ALLOW_INSTALL_HOOKS"), false),
        allow_native_deps: parse_bool(lookup("ZED_PKG_ALLOW_NATIVE_DEPS"), false),
        auth_url: lookup("ZED_PKG_AUTH_URL").filter(|value| !value.is_empty()),
        check: parse_bool(lookup("ZED_PKG_UPDATE_CHECK"), false),
        classpath: lookup("CLASSPATH").filter(|value| !value.is_empty()),
        clean: parse_bool(lookup("ZED_PKG_R2G_CLEAN"), false),
        cli: lookup("ZED_PKG_CLI").filter(|value| !value.is_empty()),
        cli_install_mode: lookup("ZED_PKG_CLI_INSTALL_MODE").filter(|value| !value.is_empty()).unwrap_or_else(|| "copy".to_string()),
        cli_target: lookup("ZED_PKG_CLI_TARGET").filter(|value| !value.is_empty()),
        comspec: lookup("COMSPEC").filter(|value| !value.is_empty()),
        display_name: lookup("ZED_PKG_AUTH_DISPLAY_NAME").filter(|value| !value.is_empty()),
        do_not_write_new_manifest: parse_bool(lookup("ZED_PKG_ALLOW_NO_MANIFEST"), false),
        docker: parse_bool(lookup("ZED_PKG_R2G_DOCKER"), false),
        dry_run: parse_bool(lookup("ZED_PKG_DRY_RUN"), false),
        email: lookup("ZED_PKG_AUTH_EMAIL").filter(|value| !value.is_empty()),
        env_config: lookup("ZED_PKG_ENV_CONFIG").filter(|value| !value.is_empty()),
        env_json: parse_bool(lookup("ZED_PKG_ENV_JSON"), false),
        env_lock: lookup("ZED_PKG_ENV_LOCK").filter(|value| !value.is_empty()),
        force: parse_bool(lookup("ZED_PKG_FORCE"), false),
        frozen: parse_bool(lookup("ZED_PKG_FROZEN"), false),
        git_submodules: parse_bool(lookup("ZED_PKG_GIT_SUBMODULES"), false),
        home: lookup("ZED_PKG_HOME").filter(|value| !value.is_empty()),
        image: lookup("ZED_PKG_R2G_IMAGE").filter(|value| !value.is_empty()).unwrap_or_else(|| "debian:stable-slim".to_string()),
        in_nix_shell: lookup("IN_NIX_SHELL").filter(|value| !value.is_empty()),
        install_mode: lookup("ZED_PKG_INSTALL_MODE").filter(|value| !value.is_empty()).unwrap_or_else(|| "symlink".to_string()),
        interactive: parse_bool(lookup("ZED_PKG_INTERACTIVE"), false),
        limit: lookup("ZED_PKG_AUDIT_LIMIT").and_then(|raw| raw.parse().ok()),
        name: lookup("ZED_PKG_NAME").filter(|value| !value.is_empty()),
        native_manager: lookup("ZED_PKG_NATIVE_MANAGER").filter(|value| !value.is_empty()),
        nix_build_top: lookup("NIX_BUILD_TOP").filter(|value| !value.is_empty()),
        older_than: lookup("ZED_PKG_GC_OLDER_THAN").filter(|value| !value.is_empty()).unwrap_or_else(|| "90d".to_string()),
        org: lookup("ZED_PKG_ORG").filter(|value| !value.is_empty()),
        out: lookup("ZED_PKG_PACK_OUT").filter(|value| !value.is_empty()),
        password_stdin: parse_bool(lookup("ZED_PKG_AUTH_PASSWORD_STDIN"), false),
        provider: lookup("ZED_PKG_AUTH_PROVIDER").filter(|value| !value.is_empty()).unwrap_or_else(|| "auto".to_string()),
        pythonpath: lookup("PYTHONPATH").filter(|value| !value.is_empty()),
        r2g_registry_mode: lookup("ZED_PKG_R2G_REGISTRY_MODE").filter(|value| !value.is_empty()).unwrap_or_else(|| "isolated".to_string()),
        r2g_root: lookup("ZED_PKG_R2G_ROOT").filter(|value| !value.is_empty()),
        registry: lookup("ZED_PKG_REGISTRY").filter(|value| !value.is_empty()).unwrap_or_else(|| "https://registry.zpkg.net".to_string()),
        runtime: lookup("ZED_PKG_R2G_RUNTIME").filter(|value| !value.is_empty()),
        skip_checksum: parse_bool(lookup("ZED_PKG_UPDATE_SKIP_CHECKSUM"), false),
        skip_vcs_checks: parse_bool(lookup("ZED_PKG_SKIP_VCS_CHECKS"), false),
        supabase_key: lookup("ZED_PKG_SUPABASE_KEY").filter(|value| !value.is_empty()),
        supabase_url: lookup("ZED_PKG_SUPABASE_URL").filter(|value| !value.is_empty()),
        target: lookup("ZED_PKG_TARGET").filter(|value| !value.is_empty()),
        token: lookup("ZED_PKG_TOKEN").filter(|value| !value.is_empty()),
        undo: parse_bool(lookup("ZED_PKG_YANK_UNDO"), false),
        xdg_config_home: lookup("XDG_CONFIG_HOME").filter(|value| !value.is_empty()),
        zed_pkg_auth_password: lookup("ZED_PKG_AUTH_PASSWORD").filter(|value| !value.is_empty()),
        zed_pkg_install_concurrency: lookup("ZED_PKG_INSTALL_CONCURRENCY").filter(|value| !value.is_empty()),
        zed_pkg_layout_config: lookup("ZED_PKG_LAYOUT_CONFIG").filter(|value| !value.is_empty()),
        zed_pkg_max_artifact_bytes: lookup("ZED_PKG_MAX_ARTIFACT_BYTES").filter(|value| !value.is_empty()),
        zed_pkg_max_binary_entries: lookup("ZED_PKG_MAX_BINARY_ENTRIES").filter(|value| !value.is_empty()),
        zed_pkg_max_registry_bytes: lookup("ZED_PKG_MAX_REGISTRY_BYTES").filter(|value| !value.is_empty()),
        zed_pkg_max_unpacked_bytes: lookup("ZED_PKG_MAX_UNPACKED_BYTES").filter(|value| !value.is_empty()),
        env_check: parse_bool(lookup("ZED_PKG_ENV_CHECK"), false),
        env_output: lookup("ZED_PKG_ENV_OUTPUT").filter(|value| !value.is_empty()),
        env_plan: lookup("ZED_PKG_ENV_PLAN").filter(|value| !value.is_empty()),
        env_receipt: lookup("ZED_PKG_ENV_RECEIPT").filter(|value| !value.is_empty()),
        env_write: parse_bool(lookup("ZED_PKG_ENV_WRITE"), false),
        zed_pkg_gc_dry_run: parse_bool(lookup("ZED_PKG_GC_DRY_RUN"), false),
        release_channel: lookup("ZED_PKG_RELEASE_CHANNEL").filter(|value| !value.is_empty()).unwrap_or_else(|| "stable".to_string()),
        release_iteration: parse_int(lookup("ZED_PKG_RELEASE_ITERATION"), 1),
        release_json: parse_bool(lookup("ZED_PKG_RELEASE_JSON"), false),
        zed_pkg_update_force: parse_bool(lookup("ZED_PKG_UPDATE_FORCE"), false),
        json: parse_bool(lookup("ZED_TASK_JSON"), false),
        plan: lookup("ZED_TASK_PLAN").filter(|value| !value.is_empty()),
        all: parse_bool(lookup("ZED_TASK_ALL"), false),
        zed_task_dry_run: parse_bool(lookup("ZED_TASK_DRY_RUN"), false),
        jobs: parse_int(lookup("ZED_TASK_JOBS"), 1),
        no_cache: parse_bool(lookup("ZED_TASK_NO_CACHE"), false),
        yes: parse_bool(lookup("ZED_TASK_YES"), false),
        zed_pkg_validate_json: parse_bool(lookup("ZED_PKG_VALIDATE_JSON"), false),
        lock: lookup("ZED_PKG_VALIDATE_LOCK").filter(|value| !value.is_empty()).unwrap_or_else(|| ".zpkg.lock".to_string()),
        manifest: lookup("ZED_PKG_VALIDATE_MANIFEST").filter(|value| !value.is_empty()).unwrap_or_else(|| ".zpkg.toml".to_string()),
        require_lock: parse_bool(lookup("ZED_PKG_VALIDATE_REQUIRE_LOCK"), false),
    }
}

/// Effectful: read the process environment.
pub fn load_from_os() -> CliEnvValues {
    load_from(|key| std::env::var(key).ok())
}

fn parse_bool(raw: Option<String>, default: bool) -> bool {
    match raw.as_deref().map(str::trim) {
        Some("1" | "true" | "TRUE" | "yes" | "YES") => true,
        Some("0" | "false" | "FALSE" | "no" | "NO") => false,
        Some(_) | None => default,
    }
}

fn parse_int(raw: Option<String>, default: i64) -> i64 {
    raw.and_then(|value| value.parse().ok()).unwrap_or(default)
}

fn parse_float(raw: Option<String>, default: f64) -> f64 {
    raw.and_then(|value| value.parse().ok()).unwrap_or(default)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct MissingEnv {
    pub name: &'static str,
    pub expected_type: &'static str,
    pub examples: &'static [&'static str],
}

impl std::fmt::Display for MissingEnv {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "missing required environment variable {}\n  expected type: {}\n  examples: {}", self.name, self.expected_type, self.examples.join(", "))
    }
}

impl std::error::Error for MissingEnv {}

fn nonempty(raw: Option<&str>) -> Option<String> {
    raw.map(str::trim).filter(|value| !value.is_empty()).map(str::to_string)
}

fn require_env(
    name: &'static str,
    expected_type: &'static str,
    examples: &'static [&'static str],
    value: Option<String>,
) -> Result<String, MissingEnv> {
    match nonempty(value.as_deref()) {
        Some(value) => Ok(value),
        None => Err(MissingEnv {
            name,
            expected_type,
            examples,
        }),
    }
}

fn pick(
    keys: &[&str],
    order: &[&str],
    shell: &std::collections::BTreeMap<String, String>,
    dotenv: &std::collections::BTreeMap<String, String>,
    flags: &std::collections::BTreeMap<String, String>,
    default: Option<&str>,
) -> Option<String> {
    for source in order {
        let map = match *source {
            "flags" => flags,
            "env_file" => dotenv,
            _ => shell,
        };
        for key in keys {
            if let Some(value) = nonempty(map.get(*key).map(String::as_str)) {
                return Some(value);
            }
        }
    }
    nonempty(default)
}

fn unquote(value: &str) -> String {
    let trimmed = value.trim();
    if trimmed.len() >= 2 {
        let bytes = trimmed.as_bytes();
        if (bytes[0] == b'"' && bytes[trimmed.len() - 1] == b'"')
            || (bytes[0] == b'\'' && bytes[trimmed.len() - 1] == b'\'')
        {
            return trimmed[1..trimmed.len() - 1].to_string();
        }
    }
    trimmed.to_string()
}

fn parse_dotenv(text: &str) -> std::collections::BTreeMap<String, String> {
    let mut out = std::collections::BTreeMap::new();
    for raw in text.lines() {
        let line = raw.trim();
        if line.is_empty() || line.starts_with('#') {
            continue;
        }
        let line = line.strip_prefix("export ").map(str::trim).unwrap_or(line);
        let Some((key, value)) = line.split_once('=') else {
            continue;
        };
        let key = key.trim();
        if key.is_empty() {
            continue;
        }
        let first = key.chars().next().unwrap_or('\0');
        if !(first.is_ascii_alphabetic() || first == '_') {
            continue;
        }
        out.insert(key.to_string(), unquote(value));
    }
    out
}

fn dotenv_enabled() -> bool {
    match std::env::var("FLAGS2ENV_DOTENV") {
        Ok(value) if matches!(value.trim(), "0" | "false" | "FALSE" | "no" | "NO") => false,
        _ => true,
    }
}

fn load_dotenv_files(files: &[&str]) -> std::collections::BTreeMap<String, String> {
    if !dotenv_enabled() {
        return std::collections::BTreeMap::new();
    }
    files.iter().fold(std::collections::BTreeMap::new(), |mut acc, path| {
        if let Ok(text) = std::fs::read_to_string(path) {
            acc.extend(parse_dotenv(&text));
        }
        acc
    })
}

fn shell_env() -> std::collections::BTreeMap<String, String> {
    std::env::vars().collect()
}

/// Resolve env-key -> value. Empty values fall through to the next source.
pub fn load_env_map(
    shell: &std::collections::BTreeMap<String, String>,
    dotenv: &std::collections::BTreeMap<String, String>,
    flags: &std::collections::BTreeMap<String, String>,
) -> Result<std::collections::BTreeMap<String, String>, MissingEnv> {
    let mut out = std::collections::BTreeMap::new();
    let adapter = pick(&["ZED_PKG_ADAPTER"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("auto"));
    if let Some(value) = adapter {
        out.insert("ZED_PKG_ADAPTER".to_string(), value);
    }
    let allow_build = pick(&["ZED_PKG_ALLOW_BUILD"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = allow_build {
        out.insert("ZED_PKG_ALLOW_BUILD".to_string(), value);
    }
    let allow_dirty = pick(&["ZED_PKG_ALLOW_DIRTY"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = allow_dirty {
        out.insert("ZED_PKG_ALLOW_DIRTY".to_string(), value);
    }
    let allow_ecosystem_mismatch = pick(&["ZED_PKG_ALLOW_ECOSYSTEM_MISMATCH"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = allow_ecosystem_mismatch {
        out.insert("ZED_PKG_ALLOW_ECOSYSTEM_MISMATCH".to_string(), value);
    }
    let allow_install_hooks = pick(&["ZED_PKG_ALLOW_INSTALL_HOOKS"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = allow_install_hooks {
        out.insert("ZED_PKG_ALLOW_INSTALL_HOOKS".to_string(), value);
    }
    let allow_native_deps = pick(&["ZED_PKG_ALLOW_NATIVE_DEPS"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = allow_native_deps {
        out.insert("ZED_PKG_ALLOW_NATIVE_DEPS".to_string(), value);
    }
    let auth_url = pick(&["ZED_PKG_AUTH_URL"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = auth_url {
        out.insert("ZED_PKG_AUTH_URL".to_string(), value);
    }
    let check = pick(&["ZED_PKG_UPDATE_CHECK"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = check {
        out.insert("ZED_PKG_UPDATE_CHECK".to_string(), value);
    }
    let classpath = pick(&["CLASSPATH"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = classpath {
        out.insert("CLASSPATH".to_string(), value);
    }
    let clean = pick(&["ZED_PKG_R2G_CLEAN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = clean {
        out.insert("ZED_PKG_R2G_CLEAN".to_string(), value);
    }
    let cli = pick(&["ZED_PKG_CLI"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = cli {
        out.insert("ZED_PKG_CLI".to_string(), value);
    }
    let cli_install_mode = pick(&["ZED_PKG_CLI_INSTALL_MODE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("copy"));
    if let Some(value) = cli_install_mode {
        out.insert("ZED_PKG_CLI_INSTALL_MODE".to_string(), value);
    }
    let cli_target = pick(&["ZED_PKG_CLI_TARGET"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = cli_target {
        out.insert("ZED_PKG_CLI_TARGET".to_string(), value);
    }
    let comspec = pick(&["COMSPEC"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = comspec {
        out.insert("COMSPEC".to_string(), value);
    }
    let display_name = pick(&["ZED_PKG_AUTH_DISPLAY_NAME"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = display_name {
        out.insert("ZED_PKG_AUTH_DISPLAY_NAME".to_string(), value);
    }
    let do_not_write_new_manifest = pick(&["ZED_PKG_ALLOW_NO_MANIFEST"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = do_not_write_new_manifest {
        out.insert("ZED_PKG_ALLOW_NO_MANIFEST".to_string(), value);
    }
    let docker = pick(&["ZED_PKG_R2G_DOCKER"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = docker {
        out.insert("ZED_PKG_R2G_DOCKER".to_string(), value);
    }
    let dry_run = pick(&["ZED_PKG_DRY_RUN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = dry_run {
        out.insert("ZED_PKG_DRY_RUN".to_string(), value);
    }
    let email = pick(&["ZED_PKG_AUTH_EMAIL"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = email {
        out.insert("ZED_PKG_AUTH_EMAIL".to_string(), value);
    }
    let env_config = pick(&["ZED_PKG_ENV_CONFIG"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = env_config {
        out.insert("ZED_PKG_ENV_CONFIG".to_string(), value);
    }
    let env_json = pick(&["ZED_PKG_ENV_JSON"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = env_json {
        out.insert("ZED_PKG_ENV_JSON".to_string(), value);
    }
    let env_lock = pick(&["ZED_PKG_ENV_LOCK"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = env_lock {
        out.insert("ZED_PKG_ENV_LOCK".to_string(), value);
    }
    let force = pick(&["ZED_PKG_FORCE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = force {
        out.insert("ZED_PKG_FORCE".to_string(), value);
    }
    let frozen = pick(&["ZED_PKG_FROZEN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = frozen {
        out.insert("ZED_PKG_FROZEN".to_string(), value);
    }
    let git_submodules = pick(&["ZED_PKG_GIT_SUBMODULES"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = git_submodules {
        out.insert("ZED_PKG_GIT_SUBMODULES".to_string(), value);
    }
    let home = pick(&["ZED_PKG_HOME"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = home {
        out.insert("ZED_PKG_HOME".to_string(), value);
    }
    let image = pick(&["ZED_PKG_R2G_IMAGE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("debian:stable-slim"));
    if let Some(value) = image {
        out.insert("ZED_PKG_R2G_IMAGE".to_string(), value);
    }
    let in_nix_shell = pick(&["IN_NIX_SHELL"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = in_nix_shell {
        out.insert("IN_NIX_SHELL".to_string(), value);
    }
    let install_mode = pick(&["ZED_PKG_INSTALL_MODE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("symlink"));
    if let Some(value) = install_mode {
        out.insert("ZED_PKG_INSTALL_MODE".to_string(), value);
    }
    let interactive = pick(&["ZED_PKG_INTERACTIVE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = interactive {
        out.insert("ZED_PKG_INTERACTIVE".to_string(), value);
    }
    let limit = pick(&["ZED_PKG_AUDIT_LIMIT"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = limit {
        out.insert("ZED_PKG_AUDIT_LIMIT".to_string(), value);
    }
    let name = pick(&["ZED_PKG_NAME"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = name {
        out.insert("ZED_PKG_NAME".to_string(), value);
    }
    let native_manager = pick(&["ZED_PKG_NATIVE_MANAGER"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = native_manager {
        out.insert("ZED_PKG_NATIVE_MANAGER".to_string(), value);
    }
    let nix_build_top = pick(&["NIX_BUILD_TOP"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = nix_build_top {
        out.insert("NIX_BUILD_TOP".to_string(), value);
    }
    let older_than = pick(&["ZED_PKG_GC_OLDER_THAN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("90d"));
    if let Some(value) = older_than {
        out.insert("ZED_PKG_GC_OLDER_THAN".to_string(), value);
    }
    let org = pick(&["ZED_PKG_ORG"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = org {
        out.insert("ZED_PKG_ORG".to_string(), value);
    }
    let out = pick(&["ZED_PKG_PACK_OUT"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = out {
        out.insert("ZED_PKG_PACK_OUT".to_string(), value);
    }
    let password_stdin = pick(&["ZED_PKG_AUTH_PASSWORD_STDIN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = password_stdin {
        out.insert("ZED_PKG_AUTH_PASSWORD_STDIN".to_string(), value);
    }
    let provider = pick(&["ZED_PKG_AUTH_PROVIDER"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("auto"));
    if let Some(value) = provider {
        out.insert("ZED_PKG_AUTH_PROVIDER".to_string(), value);
    }
    let pythonpath = pick(&["PYTHONPATH"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = pythonpath {
        out.insert("PYTHONPATH".to_string(), value);
    }
    let r2g_registry_mode = pick(&["ZED_PKG_R2G_REGISTRY_MODE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("isolated"));
    if let Some(value) = r2g_registry_mode {
        out.insert("ZED_PKG_R2G_REGISTRY_MODE".to_string(), value);
    }
    let r2g_root = pick(&["ZED_PKG_R2G_ROOT"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = r2g_root {
        out.insert("ZED_PKG_R2G_ROOT".to_string(), value);
    }
    let registry = pick(&["ZED_PKG_REGISTRY"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("https://registry.zpkg.net"));
    if let Some(value) = registry {
        out.insert("ZED_PKG_REGISTRY".to_string(), value);
    }
    let runtime = pick(&["ZED_PKG_R2G_RUNTIME"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = runtime {
        out.insert("ZED_PKG_R2G_RUNTIME".to_string(), value);
    }
    let skip_checksum = pick(&["ZED_PKG_UPDATE_SKIP_CHECKSUM"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = skip_checksum {
        out.insert("ZED_PKG_UPDATE_SKIP_CHECKSUM".to_string(), value);
    }
    let skip_vcs_checks = pick(&["ZED_PKG_SKIP_VCS_CHECKS"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = skip_vcs_checks {
        out.insert("ZED_PKG_SKIP_VCS_CHECKS".to_string(), value);
    }
    let supabase_key = pick(&["ZED_PKG_SUPABASE_KEY"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = supabase_key {
        out.insert("ZED_PKG_SUPABASE_KEY".to_string(), value);
    }
    let supabase_url = pick(&["ZED_PKG_SUPABASE_URL"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = supabase_url {
        out.insert("ZED_PKG_SUPABASE_URL".to_string(), value);
    }
    let target = pick(&["ZED_PKG_TARGET"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = target {
        out.insert("ZED_PKG_TARGET".to_string(), value);
    }
    let token = pick(&["ZED_PKG_TOKEN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = token {
        out.insert("ZED_PKG_TOKEN".to_string(), value);
    }
    let undo = pick(&["ZED_PKG_YANK_UNDO"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = undo {
        out.insert("ZED_PKG_YANK_UNDO".to_string(), value);
    }
    let xdg_config_home = pick(&["XDG_CONFIG_HOME"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = xdg_config_home {
        out.insert("XDG_CONFIG_HOME".to_string(), value);
    }
    let zed_pkg_auth_password = pick(&["ZED_PKG_AUTH_PASSWORD"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_auth_password {
        out.insert("ZED_PKG_AUTH_PASSWORD".to_string(), value);
    }
    let zed_pkg_install_concurrency = pick(&["ZED_PKG_INSTALL_CONCURRENCY"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_install_concurrency {
        out.insert("ZED_PKG_INSTALL_CONCURRENCY".to_string(), value);
    }
    let zed_pkg_layout_config = pick(&["ZED_PKG_LAYOUT_CONFIG"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_layout_config {
        out.insert("ZED_PKG_LAYOUT_CONFIG".to_string(), value);
    }
    let zed_pkg_max_artifact_bytes = pick(&["ZED_PKG_MAX_ARTIFACT_BYTES"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_max_artifact_bytes {
        out.insert("ZED_PKG_MAX_ARTIFACT_BYTES".to_string(), value);
    }
    let zed_pkg_max_binary_entries = pick(&["ZED_PKG_MAX_BINARY_ENTRIES"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_max_binary_entries {
        out.insert("ZED_PKG_MAX_BINARY_ENTRIES".to_string(), value);
    }
    let zed_pkg_max_registry_bytes = pick(&["ZED_PKG_MAX_REGISTRY_BYTES"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_max_registry_bytes {
        out.insert("ZED_PKG_MAX_REGISTRY_BYTES".to_string(), value);
    }
    let zed_pkg_max_unpacked_bytes = pick(&["ZED_PKG_MAX_UNPACKED_BYTES"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = zed_pkg_max_unpacked_bytes {
        out.insert("ZED_PKG_MAX_UNPACKED_BYTES".to_string(), value);
    }
    let env_check = pick(&["ZED_PKG_ENV_CHECK"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = env_check {
        out.insert("ZED_PKG_ENV_CHECK".to_string(), value);
    }
    let env_output = pick(&["ZED_PKG_ENV_OUTPUT"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = env_output {
        out.insert("ZED_PKG_ENV_OUTPUT".to_string(), value);
    }
    let env_plan = pick(&["ZED_PKG_ENV_PLAN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = env_plan {
        out.insert("ZED_PKG_ENV_PLAN".to_string(), value);
    }
    let env_receipt = pick(&["ZED_PKG_ENV_RECEIPT"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = env_receipt {
        out.insert("ZED_PKG_ENV_RECEIPT".to_string(), value);
    }
    let env_write = pick(&["ZED_PKG_ENV_WRITE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = env_write {
        out.insert("ZED_PKG_ENV_WRITE".to_string(), value);
    }
    let zed_pkg_gc_dry_run = pick(&["ZED_PKG_GC_DRY_RUN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = zed_pkg_gc_dry_run {
        out.insert("ZED_PKG_GC_DRY_RUN".to_string(), value);
    }
    let release_channel = pick(&["ZED_PKG_RELEASE_CHANNEL"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("stable"));
    if let Some(value) = release_channel {
        out.insert("ZED_PKG_RELEASE_CHANNEL".to_string(), value);
    }
    let release_iteration = pick(&["ZED_PKG_RELEASE_ITERATION"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("1"));
    if let Some(value) = release_iteration {
        out.insert("ZED_PKG_RELEASE_ITERATION".to_string(), value);
    }
    let release_json = pick(&["ZED_PKG_RELEASE_JSON"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = release_json {
        out.insert("ZED_PKG_RELEASE_JSON".to_string(), value);
    }
    let zed_pkg_update_force = pick(&["ZED_PKG_UPDATE_FORCE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = zed_pkg_update_force {
        out.insert("ZED_PKG_UPDATE_FORCE".to_string(), value);
    }
    let json = pick(&["ZED_TASK_JSON"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = json {
        out.insert("ZED_TASK_JSON".to_string(), value);
    }
    let plan = pick(&["ZED_TASK_PLAN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, None);
    if let Some(value) = plan {
        out.insert("ZED_TASK_PLAN".to_string(), value);
    }
    let all = pick(&["ZED_TASK_ALL"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = all {
        out.insert("ZED_TASK_ALL".to_string(), value);
    }
    let zed_task_dry_run = pick(&["ZED_TASK_DRY_RUN"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = zed_task_dry_run {
        out.insert("ZED_TASK_DRY_RUN".to_string(), value);
    }
    let jobs = pick(&["ZED_TASK_JOBS"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("1"));
    if let Some(value) = jobs {
        out.insert("ZED_TASK_JOBS".to_string(), value);
    }
    let no_cache = pick(&["ZED_TASK_NO_CACHE"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = no_cache {
        out.insert("ZED_TASK_NO_CACHE".to_string(), value);
    }
    let yes = pick(&["ZED_TASK_YES"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = yes {
        out.insert("ZED_TASK_YES".to_string(), value);
    }
    let zed_pkg_validate_json = pick(&["ZED_PKG_VALIDATE_JSON"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = zed_pkg_validate_json {
        out.insert("ZED_PKG_VALIDATE_JSON".to_string(), value);
    }
    let lock = pick(&["ZED_PKG_VALIDATE_LOCK"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some(".zpkg.lock"));
    if let Some(value) = lock {
        out.insert("ZED_PKG_VALIDATE_LOCK".to_string(), value);
    }
    let manifest = pick(&["ZED_PKG_VALIDATE_MANIFEST"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some(".zpkg.toml"));
    if let Some(value) = manifest {
        out.insert("ZED_PKG_VALIDATE_MANIFEST".to_string(), value);
    }
    let require_lock = pick(&["ZED_PKG_VALIDATE_REQUIRE_LOCK"], &["flags", "env_shell", "env_file"], shell, dotenv, flags, Some("false"));
    if let Some(value) = require_lock {
        out.insert("ZED_PKG_VALIDATE_REQUIRE_LOCK".to_string(), value);
    }
    Ok(out)
}

/// Effectful overlay: `.env` files then the process environment, ranked per key.
pub fn load_env_map_from_os() -> Result<std::collections::BTreeMap<String, String>, MissingEnv> {
    load_env_map(&shell_env(), &load_dotenv_files(&[".env"]), &std::collections::BTreeMap::new())
}
